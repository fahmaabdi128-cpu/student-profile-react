const Navbar = () => {
  return (
    <nav className="navbar">
      <h2>WatchStore</h2>

      <div>
        <a href="#">Home</a>
        <a href="#">Products</a>
        <a href="#">Contact</a>
      </div>
    </nav>
  );
};

export default Navbar;